<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag 'LOGO-LITTLE.png' %>
    <% end %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
      <li class="counter nav-item dropdown">
        <a href="#" class="nav-link" id="container-counter">
            <span class="item" id="count">
            <% if total_user_search  != nil %>
              <%= total_user_search %>
            <% else %>
            0
            <%end %>
            </span>
            <span> </span>
            <span class="item fas fa-venus-double"></span>
          </i>
        </a>
      </li>
      <li class="nav-item dropdown">
        <a href="#" class="nav-link"><i class="item far fa-bell"></i></a>
      </li>
      <li class="nav-item dropdown">
        <a href="#" class="nav-link" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="item fas fa-bars"></i></a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to "My profile", user_path(current_user), class: "dropdown-item" %>
          <%= link_to "My supports", user_path(current_user), class: "dropdown-item" %>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
        </div>
      </li>
      <% else %>
      <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
